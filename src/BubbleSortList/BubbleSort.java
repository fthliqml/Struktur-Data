package BubbleSortList;

import java.lang.reflect.Array;

public class BubbleSort {
    // Mengurutkan dari kecil-Besar atau Besar-Kecil (Looping)
    // Membandingkan 2 data dari sebelah kiri dengan data terkecil akan selalu dibandingkan

        public void display(int arr[]){
            System.out.print("[ ");
            for (int i = 0; i < arr.length; i++){
                System.out.print(arr[i] + " ");
            }
            System.out.print("]");
        }

        public void bubble(int arr[] ){
            int i, j, temp;
            boolean swapped;
            for (i = 0; i < arr.length; i++){
                temp = arr[i];


            }
        }
}
